apiVersion: v1
data:
  POSTGRES_DB: postgres
  POSTGRES_USER: postgres
  POSTGRES_HOST: immoly-postgres-service
  POSTGRES_PORT: "5432"
  STORAGE: DB
kind: ConfigMap
metadata:
  labels:
    io.kompose.service: immoly-app-env
  name: env
  namespace: immoly
