apiVersion: v1
kind: Service
metadata:
  name: immoly-postgres-service
  namespace: immoly
  labels:
    app: immoly-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: immoly-postgres
