apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: umami-db-data
  namespace: umami
  labels:
    app: umami-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
