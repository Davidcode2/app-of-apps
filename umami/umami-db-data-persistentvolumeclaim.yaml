apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: umami-db-data
  namespace: umami
  labels:
    app: umami-db
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
