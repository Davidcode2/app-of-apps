apiVersion: v1
kind: Service
metadata:
  name: umami-db
  namespace: umami
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: umami-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
