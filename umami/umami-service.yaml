apiVersion: v1
kind: Service
metadata:
  name: umami
  namespace: umami
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: umami
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
