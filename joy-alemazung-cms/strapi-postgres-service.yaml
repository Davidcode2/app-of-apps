apiVersion: v1
kind: Service
metadata:
  name: strapi-postgres-service
  namespace: joy-alemazung
  labels:
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
